---
import { siteConfig } from '../content/site';
import arteLogo from '../assets/svg/arte-logo.svg?raw';

const currentPath = Astro.url.pathname;

const logoSvg = arteLogo
  .replace(/^[\s\S]*?(?=<svg)/, '')
  .replace(/<sodipodi:namedview[\s\S]*?<\/sodipodi:namedview>\s*/g, '')
  .replace(/<defs[^>]*\/?>\s*(<\/defs>)?/g, '')
  .replace(/\s+(?:sodipodi|inkscape):[a-z:_-]+="[^"]*"/g, '')
  .replace(/\s+xmlns:(?:inkscape|sodipodi|svg)="[^"]*"/g, '')
  .replace(/\s+id="[^"]*"/g, '')
  .replace(/<path[^>]*fill:#0e0f0e[^>]*\/>/g, '')
  .replace(/fill:#ffffff/g, 'fill:#1a1814')
  .replace(/\s+width="[^"]*"/, '')
  .replace(/\s+height="[^"]*"/, '')
  .replace(/viewBox="0 0 2368\.52 2631\.8267"/, 'viewBox="0 0 2368.52 1184.88"')
  .replace('<svg', '<svg class="brand__logo" aria-hidden="true"');

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/platform', label: 'Platform' },
  { href: '/industries', label: 'Industries' },
  { href: '/contact', label: 'Contact' }
];

const isActive = (href: string) => {
  if (href === '/') return currentPath === '/' || currentPath === '';
  return currentPath.startsWith(href);
};
---

<a href="#main" class="skip-link">Skip to main content</a>

<header class="site-header" id="siteHeader">
  <div class="site-header__inner">
    <a href="/" class="brand" aria-label={`${siteConfig.name} home`}>
      <Fragment set:html={logoSvg} />
    </a>

    <nav class="primary-nav" aria-label="Primary" id="primaryNav">
      <ul class="primary-nav__list">
        {navLinks.map(link => (
          <li>
            <a href={link.href} class={isActive(link.href) ? 'is-active' : ''}>
              {link.label}
            </a>
          </li>
        ))}
      </ul>
      <a href="/contact" class="primary-nav__cta">Book a demo<span aria-hidden="true">→</span></a>
    </nav>

    <button
      class="nav-toggle"
      aria-label="Toggle navigation menu"
      aria-expanded="false"
      aria-controls="primaryNav"
      id="navToggle"
      type="button"
    >
      <span class="nav-toggle__bar" aria-hidden="true"></span>
    </button>
  </div>
</header>

<style>
  .skip-link {
    position: absolute;
    top: -100px;
    left: 0;
    background: var(--color-ink);
    color: #fff;
    padding: 0.6rem 1rem;
    z-index: 2000;
    border-radius: 0 0 8px 0;
  }
  .skip-link:focus {
    top: 0;
  }

  /* NOTE: deliberately NOT using backdrop-filter on .site-header — it
     creates a containing block that breaks position:fixed for the
     mobile drawer (.primary-nav). Use a near-opaque background instead. */
  .site-header {
    display: block;
    width: 100%;
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(247, 245, 238, 0.96);
    border-bottom: 1px solid var(--color-line);
    transition: border-color 0.25s ease, background 0.25s ease;
  }

  .site-header.is-scrolled {
    border-bottom-color: var(--color-line-strong);
    background: var(--color-bg);
    box-shadow: 0 1px 0 var(--color-line);
  }

  .site-header__inner {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 var(--space-6);
    height: var(--header-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    flex-wrap: nowrap;
  }

  /* Brand */
  .brand {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    flex-shrink: 0;
  }

  :global(.brand__logo) {
    height: 60px;
    width: auto;
    display: block;
  }

  /* Nav — desktop default */
  .primary-nav {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    flex-shrink: 0;
  }

  .primary-nav__list {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 0.25rem;
    margin: 0;
    padding: 0;
  }

  .primary-nav__list li {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .primary-nav__list a {
    display: inline-block;
    padding: 0.5rem 0.9rem;
    color: var(--color-ink-soft);
    font-size: 0.95rem;
    font-weight: 500;
    border-radius: var(--radius-pill);
    text-decoration: none;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .primary-nav__list a:hover {
    color: var(--color-ink);
    background: rgba(26, 24, 20, 0.05);
  }

  .primary-nav__list a.is-active {
    color: var(--color-accent-darker);
    background: var(--color-accent-soft);
  }

  .primary-nav__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    background: var(--color-ink);
    color: #fff;
    padding: 0.6rem 1.1rem;
    font-size: 0.88rem;
    font-weight: 600;
    border-radius: var(--radius-pill);
    text-decoration: none;
    transition: background 0.2s ease, transform 0.2s ease;
    white-space: nowrap;
  }

  .primary-nav__cta:hover {
    background: var(--color-accent);
    color: #fff;
    transform: translateY(-1px);
  }

  .primary-nav__cta span {
    transition: transform 0.2s ease;
  }
  .primary-nav__cta:hover span {
    transform: translateX(3px);
  }

  /* Hamburger toggle — desktop default: hidden */
  .nav-toggle {
    display: none;
    width: 42px;
    height: 42px;
    background: transparent;
    border: 1px solid var(--color-line-strong);
    border-radius: var(--radius-pill);
    cursor: pointer;
    align-items: center;
    justify-content: center;
    padding: 0;
    flex-shrink: 0;
  }

  .nav-toggle:hover {
    background: rgba(26, 24, 20, 0.05);
  }

  .nav-toggle__bar,
  .nav-toggle__bar::before,
  .nav-toggle__bar::after {
    display: block;
    width: 18px;
    height: 1.8px;
    background: var(--color-ink);
    border-radius: 2px;
    position: relative;
    transition: transform 0.3s ease, top 0.3s ease, background 0.2s ease;
  }

  .nav-toggle__bar::before,
  .nav-toggle__bar::after {
    content: '';
    position: absolute;
    left: 0;
  }

  .nav-toggle__bar::before { top: -6px; }
  .nav-toggle__bar::after  { top: 6px; }

  .nav-toggle.is-open .nav-toggle__bar {
    background: transparent;
  }

  .nav-toggle.is-open .nav-toggle__bar::before {
    top: 0;
    transform: rotate(45deg);
  }

  .nav-toggle.is-open .nav-toggle__bar::after {
    top: 0;
    transform: rotate(-45deg);
  }

  /* MOBILE — only below 768px */
  @media (max-width: 768px) {
    .site-header__inner {
      padding: 0 var(--space-5);
    }

    .nav-toggle {
      display: inline-flex;
    }

    .primary-nav {
      position: fixed;
      top: var(--header-height);
      left: 0;
      right: 0;
      bottom: 0;
      width: 100vw;
      height: calc(100vh - var(--header-height));
      height: calc(100dvh - var(--header-height));
      background: var(--color-bg);
      border-top: 1px solid var(--color-line);
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      padding: var(--space-6);
      transform: translateY(-110%);
      transition: transform 0.3s cubic-bezier(0.2, 0.8, 0.2, 1);
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
      z-index: 1100;
      visibility: hidden;
    }

    .primary-nav.is-open {
      transform: translateY(0);
      visibility: visible;
    }

    .primary-nav__list {
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      width: 100%;
    }

    .primary-nav__list li {
      width: 100%;
      border-bottom: 1px solid var(--color-line);
    }

    .primary-nav__list a {
      display: block;
      padding: 1rem 0.5rem;
      font-family: var(--font-display);
      font-size: 1.5rem;
      border-radius: 0;
    }

    .primary-nav__list a.is-active {
      background: transparent;
      color: var(--color-accent-dark);
    }

    .primary-nav__cta {
      margin-top: var(--space-5);
      justify-content: center;
      padding: 1rem 1.5rem;
      background: var(--color-accent);
      font-size: 1rem;
    }
  }

  /* Lock background scroll while mobile drawer is open */
  :global(body.is-menu-open) {
    overflow: hidden;
  }
</style>

<script is:inline>
  (function () {
    function init() {
      var header = document.getElementById('siteHeader');
      var toggle = document.getElementById('navToggle');
      var nav = document.getElementById('primaryNav');

      // Defensive reset on every page load (incl. bfcache restores)
      document.body.classList.remove('is-menu-open');
      if (nav) nav.classList.remove('is-open');
      if (toggle) {
        toggle.classList.remove('is-open');
        toggle.setAttribute('aria-expanded', 'false');
      }

      if (toggle && nav) {
        toggle.addEventListener('click', function (e) {
          e.preventDefault();
          var isOpen = nav.classList.toggle('is-open');
          toggle.classList.toggle('is-open', isOpen);
          toggle.setAttribute('aria-expanded', String(isOpen));
          document.body.classList.toggle('is-menu-open', isOpen);
        });

        var links = nav.querySelectorAll('a');
        for (var i = 0; i < links.length; i++) {
          links[i].addEventListener('click', function () {
            nav.classList.remove('is-open');
            toggle.classList.remove('is-open');
            toggle.setAttribute('aria-expanded', 'false');
            document.body.classList.remove('is-menu-open');
          });
        }

        // Close drawer if viewport is resized to desktop width
        window.addEventListener('resize', function () {
          if (window.innerWidth > 768 && nav.classList.contains('is-open')) {
            nav.classList.remove('is-open');
            toggle.classList.remove('is-open');
            toggle.setAttribute('aria-expanded', 'false');
            document.body.classList.remove('is-menu-open');
          }
        });
      }

      if (header) {
        var onScroll = function () {
          if (window.scrollY > 4) header.classList.add('is-scrolled');
          else header.classList.remove('is-scrolled');
        };
        onScroll();
        window.addEventListener('scroll', onScroll, { passive: true });
      }
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }

    // Reset state when the page is restored from bfcache (back/forward)
    window.addEventListener('pageshow', function (e) {
      if (e.persisted) init();
    });
  })();
</script>
